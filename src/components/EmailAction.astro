---
import { email } from "../data/contact";

interface Props {
  class?: string;
  label?: string;
}

const { class: className, label = email } = Astro.props;
---

<a
  class={className}
  href={`mailto:${email}`}
  data-copy-email
  data-email={email}
  data-idle-label={label}
>{label}</a>
